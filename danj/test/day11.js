const expect = require('chai').expect
const helpers = require('../helpers').default
const day11 = require(`../day11`).default

describe(`day 11`, () => {
  let input, expectedResult
  
  describe('part A', () => {
    // refactor
  })
})